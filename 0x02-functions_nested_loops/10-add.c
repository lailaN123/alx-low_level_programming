#include"main.h"
#include <stdio.h>
/**
 * add - adds two integers and returns the result.
 * @a:The first integer.
 * @b:The second integer.
 * Return: the sum of the two integers.
 */
int add(int a, int b)
{
	return (a + b);
}
